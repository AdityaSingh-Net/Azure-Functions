# Azure-Functions
Developed an Azure Function using C# that includes two endpoints: a greeting endpoint and a simple in-memory to-do list API with support for GET and POST methods. The function processes JSON input and returns dynamic responses, demonstrating integration with GitHub for cloud-based event handling.
